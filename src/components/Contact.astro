---
import { contactEmail } from '../content/site';

const endpoint = import.meta.env.PUBLIC_CONTACT_FUNCTION_URL ?? '';
---

<section class="section contact" id="contact" aria-labelledby="contact-title">
  <div class="container contact__grid">
    <div class="contact__intro" data-reveal>
      <p class="section-kicker">Contact</p>
      <h2 id="contact-title">Tell me about your site.</h2>
      <p>What do you photograph, and what do you need the website to do?</p>
      <div class="contact__email">
        <span>Email directly</span>
        <a href={`mailto:${contactEmail}`}>{contactEmail}</a>
      </div>
      <p class="contact__reply"><span aria-hidden="true">↳</span> Replies come directly from Leon.</p>
    </div>

    <form
      class="contact-form"
      action={`mailto:${contactEmail}`}
      method="post"
      enctype="text/plain"
      data-contact-form
      data-endpoint={endpoint}
      data-reveal
    >
      <div class="form-row">
        <div class="field">
          <label for="contact-name">Name</label>
          <input id="contact-name" name="name" type="text" autocomplete="name" minlength="2" maxlength="80" required aria-describedby="error-name" />
          <span class="field__error" id="error-name" data-error-for="name"></span>
        </div>
        <div class="field">
          <label for="contact-email">Email</label>
          <input id="contact-email" name="email" type="email" autocomplete="email" maxlength="254" required aria-describedby="error-email" />
          <span class="field__error" id="error-email" data-error-for="email"></span>
        </div>
      </div>

      <div class="field">
        <label for="contact-focus">What do you photograph?</label>
        <input id="contact-focus" name="focus" type="text" placeholder="Weddings, portraits, commercial…" minlength="2" maxlength="80" required aria-describedby="error-focus" />
        <span class="field__error" id="error-focus" data-error-for="focus"></span>
      </div>

      <div class="field">
        <label for="contact-message">What are you looking for?</label>
        <textarea id="contact-message" name="message" rows="5" minlength="20" maxlength="2000" required aria-describedby="error-message"></textarea>
        <span class="field__error" id="error-message" data-error-for="message"></span>
      </div>

      <div class="honeypot" aria-hidden="true">
        <label for="contact-company">Company website</label>
        <input id="contact-company" name="company" type="text" tabindex="-1" autocomplete="off" />
      </div>

      <div class="contact-form__footer">
        <button class="button button--ink" type="submit">Send inquiry <span aria-hidden="true">↗</span></button>
        <p>By sending this, you agree that Leon may reply about your project. No mailing list.</p>
      </div>
      <p class="form-status" data-form-status role="status" aria-live="polite"></p>
    </form>
  </div>
</section>
