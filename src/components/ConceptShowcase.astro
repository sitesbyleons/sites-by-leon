---
import WebsiteConcept from './WebsiteConcept.astro';
import { concepts } from '../content/site';
---

<section class="section work" id="work" aria-labelledby="work-title">
  <div class="container">
    <header class="section-head" data-reveal>
      <p class="section-kicker">Three website concepts</p>
      <h2 id="work-title">Made to show <em>your work.</em></h2>
    </header>

    <div class="portfolio-list">
      {concepts.map((concept) => <WebsiteConcept concept={concept} />)}
    </div>
  </div>
</section>
