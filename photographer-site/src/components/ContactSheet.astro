---
import SelectedWorkReel from './SelectedWorkReel';
import type { Gallery } from '../lib/content/types';

interface Props {
  galleries: Gallery[];
  tone: 'editorial' | 'athletic' | 'modern';
}

const { galleries, tone } = Astro.props;
---

<SelectedWorkReel galleries={galleries} tone={tone} client:visible />
